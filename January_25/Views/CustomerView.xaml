<UserControl x:Class="January_25.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:January_25.Views"
             xmlns:model="clr-namespace:January_25.Models"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Control.IsFocused" Value="True">
                    <Setter Property="Control.Foreground" Value="Red"/>
                    <Setter Property="Control.Background" Value="Green"/>
                    <Setter Property="Control.BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <TextBox Margin="10" HorizontalContentAlignment="Stretch" Text="{Binding Path=FirstName}"></TextBox>
            <TextBox Margin="10" HorizontalContentAlignment="Stretch" Text="{Binding Path=LastName}"></TextBox>
            <TextBox Margin="10" HorizontalContentAlignment="Stretch" Text="{Binding Path=City}"></TextBox>
        </StackPanel>
    </Grid>
</UserControl>
