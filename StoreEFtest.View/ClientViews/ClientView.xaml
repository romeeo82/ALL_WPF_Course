<UserControl x:Class="StoreEFtest.View.ClientViews.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:StoreEFtest.View.ClientViews"
             xmlns:viewModels="clr-namespace:StoreEFtest.ViewModel;assembly=StoreEFtest.ViewModel"           
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <viewModels:ClientViewModel x:Key="ClientViewModel"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <Binding Source="{StaticResource ClientViewModel}"/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <views:ProductsView DataContext="{Binding ProductsViewModel}" Grid.Column="0" Margin="10"/>
        <views:ProductDetailedView DataContext="{Binding ProductsViewModel.SelectedProduct}" Grid.Column="1" Margin="10"/>
    </Grid>
</UserControl>
